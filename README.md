## DSA CODES

*********************************************************
<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
</head>
<body>

  <h2>1. Reverse a String</h2>
    <p><strong>Problem</strong>: Reverse the given string.</p>

  <p><strong>Sample Input & Output:</strong></p>

  <p><strong>1.</strong> Input:</p>
    <pre><code>hello</code></pre>
    <p>Output:</p>
    <pre><code>olleh</code></pre>

   <p><strong>2.</strong> Input:</p>
    <pre><code>world</code></pre>
    <p>Output:</p>
    <pre><code>dlrow</code></pre>

  <p><strong>3.</strong> Input:</p>
    <pre><code>data</code></pre>
    <p>Output:</p>
    <pre><code>atad</code></pre>

  <h2>2. Check if a Number is Prime</h2>
  <p><strong>Problem</strong>: Determine if the given number is a prime number.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>7</code></pre>
  <p>Output:</p>
  <pre><code>True</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>10</code></pre>
  <p>Output:</p>
  <pre><code>False</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>13</code></pre>
  <p>Output:</p>
  <pre><code>True</code></pre>

  <h2>3. Find Maximum in an Array</h2>
  <p><strong>Problem</strong>: Find the largest element in an array.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[1, 3, 5, 2, 4]</code></pre>
  <p>Output:</p>
  <pre><code>5</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[9, 7, 6]</code></pre>
  <p>Output:</p>
  <pre><code>9</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[-1, -3, -2]</code></pre>
  <p>Output:</p>
  <pre><code>-1</code></pre>

  <h2>4. Combine Two  Sorted Array </h2>
  <p><strong>Problem</strong>: Combaine  the  Two Array  into One Single Array </p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[1, 2, 3, 4] [5,6] </code></pre>
  <p>Output:</p>
  <pre><code>[1, 2, 3, 4, 5, 6]</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[2, 1] [9, 6]</code></pre>
  <p>Output:</p>
  <pre><code>[2, 1, 9, 6]</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[5, 5, 6, 7] [ 0, 7, 6]</code></pre>
  <p>Output:</p>
  <pre><code>[5, 5, 6, 7, 0, 7, 6]</code></pre>

  <h2>5. Palindrome String</h2>
  <p><strong>Problem</strong>: Check if the given string is a palindrome.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>madam</code></pre>
  <p>Output:</p>
  <pre><code>True</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>hello</code></pre>
  <p>Output:</p>
  <pre><code>False</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>racecar</code></pre>
  <p>Output:</p>
  <pre><code>True</code></pre>

  <h2>6. Find Second Largest Element in an Array</h2>
  <p><strong>Problem</strong>: Find the second largest element in an array.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[3, 1, 4, 2]</code></pre>
  <p>Output:</p>
  <pre><code>3</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[7, 8, 8, 6]</code></pre>
  <p>Output:</p>
  <pre><code>7</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[10, 5, 20]</code></pre>
  <p>Output:</p>
  <pre><code>10</code></pre>

  <h2>7. Remove Duplicates from a Sorted Array</h2>
  <p><strong>Problem</strong>: Remove duplicates from a sorted array and return the length of the new array.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[1, 1, 2, 3]</code></pre>
  <p>Output:</p>
  <pre><code>3</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[0, 0, 1, 2, 2]</code></pre>
  <p>Output:</p>
  <pre><code>3</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[1, 2, 3, 4]</code></pre>
  <p>Output:</p>
  <pre><code>4</code></pre>

  <h2>8. Move Zeros to End</h2>
  <p><strong>Problem</strong>: Move all zeros in the array to the end while maintaining the relative order of other elements.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[0, 1, 0, 3, 12]</code></pre>
  <p>Output:</p>
  <pre><code>[1, 3, 12, 0, 0]</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[1, 2, 0, 0, 5]</code></pre>
  <p>Output:</p>
  <pre><code>[1, 2, 5, 0, 0]</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[0, 0, 1]</code></pre>
  <p>Output:</p>
  <pre><code>[1, 0, 0]</code></pre>

  <h2>9. Find Missing Number</h2>
  <p><strong>Problem</strong>: Find the missing number in an array of size n-1 containing elements from 1 to n.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[1, 2, 4, 5]</code></pre>
  <p>Output:</p>
  <pre><code>3</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[2, 3, 1]</code></pre>
  <p>Output:</p>
  <pre><code>4</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[6, 4, 5, 3]</code></pre>
  <p>Output:</p>
  <pre><code>2</code></pre>

  <h2>10. Intersection of Two Arrays</h2>
  <p><strong>Problem</strong>: Find the intersection of two arrays.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[1, 2, 3], [2, 3, 4]</code></pre>
  <p>Output:</p>
  <pre><code>[2, 3]</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[4, 5], [5, 6]</code></pre>
  <p>Output:</p>
  <pre><code>[5]</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[1, 7, 9], [2, 7]</code></pre>
  <p>Output:</p>
  <pre><code>[7]</code></pre>

  <h2>11. Check Anagram</h2>
  <p><strong>Problem</strong>: Check if two strings are anagrams of each other.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>"listen", "silent"</code></pre>
  <p>Output:</p>
  <pre><code>True</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>"triangle", "integral"</code></pre>
  <p>Output:</p>
  <pre><code>True</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>"hello", "world"</code></pre>
  <p>Output:</p>
  <pre><code>False</code></pre>

  <h2>12. Find the Majority Element</h2>
  <p><strong>Problem</strong>: Find the majority element in an array (the element that appears more than half the time).</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[3, 3, 4, 2, 3]</code></pre>
  <p>Output:</p>
  <pre><code>3</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[2, 2, 1, 1, 2]</code></pre>
  <p>Output:</p>
  <pre><code>2</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[1, 2, 3]</code></pre>
  <p>Output:</p>
  <pre><code>None</code></pre>

  <h2>13. Find the First Unique Character in a String</h2>
  <p><strong>Problem</strong>: Find the first non-repeating character in a string.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>"leetcode"</code></pre>
  <p>Output:</p>
  <pre><code>'l'</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>"loveleetcode"</code></pre>
  <p>Output:</p>
  <pre><code>'v'</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>"aabb"</code></pre>
  <p>Output:</p>
  <pre><code>None</code></pre>

  <h2>14. Longest Common Prefix</h2>
  <p><strong>Problem</strong>: Find the longest common prefix string amongst an array of strings.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>["flower", "flow", "flight"]</code></pre>
  <p>Output:</p>
  <pre><code>"fl"</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>["dog", "racecar", "car"]</code></pre>
  <p>Output:</p>
  <pre><code>""</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>["interview", "interval"]</code></pre>
  <p>Output:</p>
  <pre><code>"inter"</code></pre>

  <h2>15. Remove Vowels from a String</h2>
  <p><strong>Problem</strong>: Remove all vowels from the given string.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>"hello"</code></pre>
  <p>Output:</p>
  <pre><code>"hll"</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>"algorithm"</code></pre>
  <p>Output:</p>
  <pre><code>"lgrthm"</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>"data"</code></pre>
  <p>Output:</p>
  <pre><code>"dt"</code></pre>

  <h2>16. Two Sum</h2>
  <p><strong>Problem</strong>: Find two numbers in the array that add up to a given target.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[2, 7, 11, 15], target = 9</code></pre>
  <p>Output:</p>
  <pre><code>[2, 7]</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[3, 2, 4], target = 6</code></pre>
  <p>Output:</p>
  <pre><code>[2, 4]</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[1, 3, 5], target = 4</code></pre>
  <p>Output:</p>
  <pre><code>[1, 3]</code></pre>

  <h2>17. Find Duplicates in an Array</h2>
  <p><strong>Problem</strong>: Find the duplicate numbers in an array.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[1, 2, 3, 4, 2]</code></pre>
  <p>Output:</p>
  <pre><code>2</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[3, 3, 1]</code></pre>
  <p>Output:</p>
  <pre><code>3</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[1, 5, 5, 6, 6]</code></pre>
  <p>Output:</p>
  <pre><code>5, 6</code></pre>

  <h2>18. Count Occurrences of a Character</h2>
  <p><strong>Problem</strong>: Count the occurrences of a specific character in a string.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>"hello", 'l'</code></pre>
  <p>Output:</p>
  <pre><code>2</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>"banana", 'a'</code></pre>
  <p>Output:</p>
  <pre><code>3</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>"apple", 'p'</code></pre>
  <p>Output:</p>
  <pre><code>2</code></pre>

  <h2>19. Find Factorial of a Number </h2>
  <p><strong>Problem</strong>: Find the factorial of a given number. </p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code> 5</code></pre>
  <p>Output:</p>
  <pre><code>120</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code> 4 </code></pre>
  <p>Output:</p>
  <pre><code>24</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>6</code></pre>
  <p>Output:</p>
  <pre><code> 720 </code></pre>

  <h2>20. Max Occurrences of  Digit in  a Number </h2>
  <p><strong>Problem</strong>: To Find the  Particular Digit  can be Most occurrence  in a  Number </p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>12312345
     </code></pre>
  <p>Output:</p>
  <pre><code>1</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>10203040</code></pre>
  <p>Output:</p>
  <pre><code>0</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>57999</code></pre>
  <p>Output:</p>
  <pre><code>9</code></pre>

  <h2>21. Merge Two Sorted Arrays </h2>
  <p><strong>Problem</strong>: Merge two sorted arrays into one sorted array.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[1, 3, 5], [2, 4, 6] </code></pre>
  <p>Output:</p>
  <pre><code>[1, 2, 3, 4, 5, 6]</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code> [0, 1], [3, 4] </code></pre>
  <p>Output:</p>
  <pre><code>[0, 1, 3, 4]</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[5, 7], [1, 9]</code></pre>
  <p>Output:</p>
  <pre><code> [1, 5, 7, 9]</code></pre>

   <h2>22. Find the GCD of Two Numbers</h2>
  <p><strong>Problem</strong>: Find the greatest common divisor (GCD) of two numbers.</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>8, 12</code></pre>
  <p>Output:</p>
  <pre><code>4</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>54, 24</code></pre>
  <p>Output:</p>
  <pre><code>6</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>9, 28</code></pre>
  <p>Output:</p>
  <pre><code>1</code></pre>
  

  <h2>23. Check Power of Two </h2>
  <p><strong>Problem</strong>: Determine if the given number is a power of two. </p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>8</code></pre>
  <p>Output:</p>
  <pre><code> True</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>10</code></pre>
  <p>Output:</p>
  <pre><code>False</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>16</code></pre>
  <p>Output:</p>
  <pre><code>true </code></pre>

  <h2>24.Find the Sum of Digits of a Number </h2>
  <p><strong>Problem</strong>: Find the sum of the digits of a number. </p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>123</code></pre>
  <p>Output:</p>
  <pre><code>6</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>456</code></pre>
  <p>Output:</p>
  <pre><code>15</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>789</code></pre>
  <p>Output:</p>
  <pre><code>24</code></pre>


  <h2>25.Find Maximum Subarray Sum (Kadane's Algorithm) </h2>
  <p><strong>Problem</strong>: Find the maximum sum of a contiguous subarray.
</p>
  <p><strong>Sample Input & Output:</strong></p>
  <p><strong>1.</strong> Input:</p>
  <pre><code>[-2, 1, -3, 4, -1, 2, 1, -5, 4] </code></pre>
  <p>Output:</p>
  <pre><code>6</code></pre>
  <p><strong>2.</strong> Input:</p>
  <pre><code>[1, 2, 3, 4] </code></pre>
  <p>Output:</p>
  <pre><code>10</code></pre>
  <p><strong>3.</strong> Input:</p>
  <pre><code>[-1, -2, -3, -4]</code></pre>
  <p>Output:</p>
  <pre><code>-1</code></pre>

 

</body>
</html>
